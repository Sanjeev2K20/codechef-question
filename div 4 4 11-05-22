#include<iostream>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int a,b,m;
        cin>>a>>b>>m;
        int A=abs(a-b);
        int B=m-A;
        int C=min(A,B);
        cout<<C<<endl;
        
    }
}
